import { PaginationConfig } from './pagination-config.model';

describe('PaginationConfig', () => {
  it('should create an instance', () => {
    expect(new PaginationConfig()).toBeTruthy();
  });
});
